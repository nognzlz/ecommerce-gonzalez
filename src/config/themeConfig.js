import { createTheme } from '@mui/material/styles';

const theme = createTheme({
    palette: {
        primary: {
            main: '#FFCC1D',
        },
        secondary: {
            main: '#0B4619',
        },
        neutral: {
            main: '#E8E8CC',
        },
    },
});

export default theme;
