import Container from '../UI/Container';
import React from 'react';
import { Routes } from '../routes';

export const Main = () => {
    return (
        <Container>
            <Routes />
        </Container>
    );
};

export default Main;
